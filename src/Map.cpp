#include "DTM.h"






/*Map::Map(std::vector<PJ_XYZ> Coords)
{
	m_Coords = Coords;
}

void Map::Triang_Del()
{
	for(std::vector<PJ_XYZ>::iterator it = m_Coords.begin(); it != m_Coords.end() ; ++it)
	    {
	    	//vInputPoints.push_back();
	    	dt.insert(Point2(it->x, it->y,it->z)); 
	    }
    
}

Fade_2D Map::get_dt()
{
	return dt;
}

void Map::get_height(double x, double y, double z)
{
	dt.getHeight(x,y,z,NULL,0);
}*/
